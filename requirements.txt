# requirements.txt — Python 3.11 target (no pinned interpreter; 3.11 per __pycache__).
#
# ВАЖНО: установка зависимостей — действие, требующее одобрения оператора
# (см. VALIDATION.md). Не запускать `pip install -r requirements.txt` как
# рутинную валидацию.
#
# Playwright дополнительно требует установить сам браузер (бот использует
# channel="chrome", т.е. настоящий Chrome):
#     playwright install chrome
#
# Версии — умеренные нижние границы, выведенные из реально используемых API;
# точные версии не зафиксированы, т.к. в дереве нет установленного окружения.

# --- Ядро бота (flow_bot.py) ---
aiogram>=3.24        # Bot API 9.4: поле style у inline-кнопок (зелёная кнопка выбора в визарде)
aiohttp>=3.9         # прямые HTTP-вызовы Flow API (FlowHttpClient) + сессия aiogram
playwright>=1.40     # SessionKeeper: живой Chrome-профиль (перехват Bearer/reCAPTCHA); channel="chrome"
python-dotenv>=1.0   # load_dotenv(".env") в flow_bot.py и tools/*.py

# --- Опциональные (импортируются лениво, нужны только при включённой фиче) ---
aiohttp-socks        # ProxyConnector: SOCKS-прокси для Telegram API (TG_PROXY_URL)
2captcha-python      # `from twocaptcha import TwoCaptcha` — платный решатель (CAPTCHA_PROVIDER=2captcha, TWOCAPTCHA_KEY)
telethon             # только для tg_e2e/ (telegram_bot_tester.py, внешние режимы E2E-тестера)

# Стандартная библиотека покрывает остальное: sqlite3 (metrics.py), json, re,
# asyncio и т.д. flow_profiler/ и flow_core.py — stdlib-only (playwright у
# профайлера импортируется лениво только в браузерных режимах).
